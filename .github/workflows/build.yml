name: CodeMate Mobile CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  schedule:
    # 每周一凌晨2点运行完整测试
    - cron: '0 2 * * 1'

jobs:
  code-quality:
    name: 代码质量检查
    runs-on: ubuntu-latest
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: 设置JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: 设置Android SDK
      uses: android-actions/setup-android@v3
      with:
        api-level: 34
        build-tools-version: 34.0.0
        target: android-34

    - name: 缓存Gradle
      uses: actions/cache@v3
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: 运行Detekt静态分析
      run: ./gradlew detekt

    - name: 运行ktlint代码风格检查
      run: ./gradlew ktlint

    - name: 上传Detekt报告
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: detekt-report
        path: build/reports/detekt/

  unit-tests:
    name: 单元测试
    runs-on: ubuntu-latest
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 设置JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: 设置Android SDK
      uses: android-actions/setup-android@v3
      with:
        api-level: 34
        build-tools-version: 34.0.0
        target: android-34

    - name: 缓存Gradle
      uses: actions/cache@v3
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: 运行单元测试
      run: ./gradlew test

    - name: 生成测试覆盖率报告
      run: ./gradlew jacocoTestReport

    - name: 上传测试报告
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-reports
        path: |
          app/build/reports/tests/testDebugUnitTest/
          app/build/reports/jacoco/

  build-debug:
    name: 构建Debug APK
    runs-on: ubuntu-latest
    needs: [code-quality, unit-tests]
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 设置JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: 设置Android SDK
      uses: android-actions/setup-android@v3
      with:
        api-level: 34
        build-tools-version: 34.0.0
        target: android-34

    - name: 缓存Gradle
      uses: actions/cache@v3
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: 构建Debug APK
      run: ./gradlew assembleDebug

    - name: 上传Debug APK
      uses: actions/upload-artifact@v4
      with:
        name: app-debug-apk
        path: app/build/outputs/apk/debug/app-debug.apk

  build-release:
    name: 构建Release APK
    runs-on: ubuntu-latest
    needs: [code-quality, unit-tests]
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 设置JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: 设置Android SDK
      uses: android-actions/setup-android@v3
      with:
        api-level: 34
        build-tools-version: 34.0.0
        target: android-34

    - name: 缓存Gradle
      uses: actions/cache@v3
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: 构建Release APK (仅在main分支)
      if: github.ref == 'refs/heads/main'
      run: ./gradlew assembleRelease

    - name: 上传Release APK
      if: github.ref == 'refs/heads/main'
      uses: actions/upload-artifact@v4
      with:
        name: app-release-apk
        path: app/build/outputs/apk/release/app-release.apk

  integration-tests:
    name: 集成测试
    runs-on: macos-latest
    needs: [build-debug]
    if: github.event_name == 'pull_request'
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 设置JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: 设置Android SDK
      uses: android-actions/setup-android@v3
      with:
        api-level: 34
        build-tools-version: 34.0.0
        target: android-34

    - name: 启动Android模拟器
      uses: reactivecircus/android-emulator-runner@v2
      with:
        api-level: 30
        target: google_apis
        arch: x86_64
        profile: pixel_4
        script: adb shell input keyevent 82

    - name: 等待模拟器启动
      run: adb wait-for-device && adb shell input keyevent 82

    - name: 安装Debug APK
      run: adb install app/build/outputs/apk/debug/app-debug.apk

    - name: 运行集成测试
      run: ./gradlew connectedAndroidTest

    - name: 上传测试报告
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: integration-test-reports
        path: app/build/reports/androidTests/connected/

  security-scan:
    name: 安全扫描
    runs-on: ubuntu-latest
    needs: [build-debug]
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 下载Debug APK
      uses: actions/download-artifact@v4
      with:
        name: app-debug-apk
        path: app-debug.apk

    - name: 运行MobSF静态分析
      uses: ajaybathini/mobile-security-framework-mobsf@master
      with:
        api_key: ${{ secrets.MOBSF_API_KEY }}
        file: app-debug.apk

    - name: 运行Sonar扫描 (可选)
      if: github.event_name == 'pull_request'
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      run: |
        ./gradlew sonarqube \
          -Dsonar.projectKey=your-org/codemate-mobile \
          -Dsonar.host.url=${{ secrets.SONAR_HOST_URL }} \
          -Dsonar.login=${{ secrets.SONAR_TOKEN }}

  performance-tests:
    name: 性能测试
    runs-on: ubuntu-latest
    needs: [build-debug]
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 设置JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: 设置Android SDK
      uses: android-actions/setup-android@v3
      with:
        api-level: 34
        build-tools-version: 34.0.0
        target: android-34

    - name: 运行性能基准测试
      run: ./gradlew benchmark

    - name: 上传性能报告
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: performance-reports
        path: app/build/reports/benchmark/

  deploy:
    name: 部署
    runs-on: ubuntu-latest
    needs: [build-release, integration-tests, security-scan, performance-tests]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 下载Release APK
      uses: actions/download-artifact@v4
      with:
        name: app-release-apk
        path: app-release.apk

    - name: 部署到Firebase App Distribution
      uses: wzieba/Firebase-Distribution-Github-Action@v1
      with:
        appId: ${{ secrets.FIREBASE_APP_ID }}
        serviceCredentialsFileContent: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        groups: testers
        file: app-release.apk

    - name: 创建GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v1.0.0-${{ github.sha }}
        name: CodeMate Mobile v1.0.0
        body: |
          ## 🎉 CodeMate Mobile v1.0.0 Build
          
          **构建信息:**
          - Commit: ${{ github.sha }}
          - 构建时间: ${{ github.event.head_commit.timestamp }}
          - 构建者: ${{ github.actor }}
          
          **主要更新:**
          - ✅ 完整的AI编程助手功能
          - ✅ 智能代码编辑器
          - ✅ 本地编译引擎
          - ✅ GitHub集成
          - ✅ 企业级安全保护
          
          **下载链接:**
          - [APK下载](https://github.com/${{ github.repository }}/releases/download/v1.0.0-${{ github.sha }}/app-release.apk)
        files: app-release.apk
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: 发送部署通知
      uses: 8398a7/action-slack@v3
      with:
        status: ${{ job.status }}
        channel: '#deployments'
        webhook_url: ${{ secrets.SLACK_WEBHOOK }}
        message: |
          🚀 CodeMate Mobile部署成功！
          
          **版本:** v1.0.0-${{ github.sha }}
          **分支:** ${{ github.ref_name }}
          **构建者:** ${{ github.actor }}
          
          [查看Release](https://github.com/${{ github.repository }}/releases)
      if: always()

  cleanup:
    name: 清理资源
    runs-on: ubuntu-latest
    needs: [deploy]
    if: always()
    
    steps:
    - name: 清理工作空间
      run: |
        echo "清理构建缓存..."
        docker system prune -f
        rm -rf ~/.gradle/caches/
        echo "清理完成"